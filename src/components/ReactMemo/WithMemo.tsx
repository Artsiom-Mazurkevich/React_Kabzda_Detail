import React, {useState} from 'react';

export const WithMemo = React.memo((props: { test: Array<string> }) => {

    const [names, setNames] = useState()

    return (
        <div>

        </div>
    );
})

